<configuration>
    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{0} - %M - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <!-- <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/application.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender> -->

    <!-- <logger name="com.turner.poker.AnalysisEngine" level="debug">
        <appender-ref ref="CONSOLE" />
    </logger>

    <logger name="com.turner.poker.ForcedCardsTestDriver" level="info">
        <appender-ref ref="CONSOLE" />
    </logger> -->

        <logger name="com.turner.poker" level="debug">
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- Root Logger -->
    <!-- <root level="error">
        <appender-ref ref="CONSOLE"/>
        <<appender-ref ref="FILE"/>>
    </root> -->
</configuration>
